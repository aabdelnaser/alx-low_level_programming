#include "main.h"

/**
 * main - Program that prints all numbers of base 10
 *
 *
 * Return: Always 0 (Success)
 * Name: Ahmed Abdel Nasser
 * ALX Software Engineering Cohort 12
 */
int _isdigit(int num)
{

	if (num >= 48 && num <= 57)
	{
		_putchar(num);
	}
	_putchar('\n');

	return (0);
}
